# environment.yml
# conda env create -f penv.yaml
name: penv
channels:
  - defaults
dependencies:
  - python
  - ipykernel
  - pandas
  - numpy
  - matplotlib
  - scikit-learn
  - pip
  - pillow
  - pyspark
  - pymongo
  - pytest
  - seaborn
  - tensorflow
  - streamlit

